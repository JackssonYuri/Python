Ler uma temperatura em graus Celsius e apresentá-la convertida em graus Fahrenheit. A fórmula de conversão é F = (9 * C + 160) / 5, na qual F é a temperatura em Fahrenheit e C é a temperatura em graus Celsius
- Função para ler e retorna o valor da temperatura (não recebe parâmetro)
- Função para fazer o cálculo (recebe como parâmetro a temperatura em graus Celsius)
- Função para mostrar o resultado, recebendo como parâmetro o valor e fazendo a impressão

#Function Celseius to Fahrenheit

def celsius_to_fahrenheit():
    C = float(input("Temperatura em Celsius: "))

    return C

def calculo(C):
     fahrenheit = (9 * C + 160)/5
    
     return fahrenheit

def resultado(fahrenheit):
     print(fahrenheit)

temperatura_c = celsius_to_fahrenheit()
temperatura_f = calculo(temperatura_c)
mostrar = resultado(temperatura_f)

